import React from 'react'
import Layout from '../components/layout'

function NotFound() {
    return (
        <Layout>
            <h1>That things that YOU asked for...</h1>
            <p style={{ fontSize: 24, fontWeight: 400 }}>is nowhere to be found!</p>
        </Layout>
    )
}

export default NotFound
